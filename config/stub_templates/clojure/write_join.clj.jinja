{%- set_global out = [] -%}
{%- for term in terms -%}
  {%- if term.var_type -%}
    {%- set ident = term.ident -%}
  {%- else -%}
    {%- set ident = '"' ~ term.ident ~ '"' -%}
  {%- endif -%}
  {%- set_global out = out | concat(with=ident) -%}
{%- endfor -%}

{%- for line in output_comments -%};; {{ line }}
{% endfor -%}
(print {{ out | join(sep=" ") }})
(output "")
